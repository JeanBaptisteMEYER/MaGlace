package com.jbm.maglace.di

/*
@Module
@InstallIn(SingletonComponent::class)
class MyLocationModule {
    //This will provide the Location Service instance for hilt injections
    @Provides
    @ActivityContext
    fun provideLocationService(activity: Context): LocationService = LocationService(activity)
}

 */